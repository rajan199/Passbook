<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ws.wolfsoft.creative">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signin" />
        <activity android:name=".signup" />
        <activity
            android:name=".Drawer_act"
            android:label="@string/title_activity_drawer_act"
            android:theme="@style/AppTheme" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".My_profile" />
        <activity android:name=".Balance_sheet" />
        <activity android:name=".Expense" />
        <activity android:name=".Income"></activity>
    </application>

</manifest>